ENV=prod # Options: localdev, ci, dev, int, prod

LOGLEVEL=info # Options: debug, info, warn, error
LOGFORMAT=text # Options: text, json

DB_HOST=database
DB_PORT=5432
DB_USER=stock
DB_PASSWORD=password
DB_NAME=stockdb
DB_SSLMODE=disable
